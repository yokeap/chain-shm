# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Debug / generated pipeline output
debug_seg/
debug/

# NOTE: debug_v2/ is intentionally tracked — it holds the per-process report
# figures + wear reports needed to build the research report on another machine.
